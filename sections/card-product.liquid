{{ 'section-card-product.css' | asset_url | stylesheet_tag }}

<div class="section-card-product">
  <div class="page-width">
    <div class="row">
      {% for block in section.blocks %}
        {% assign products = block.settings.product_item %}
        {% if products != blank %}
          {% for product in products %}
            <div class="col-3" {{ block.shopify_attributes }}>
              <div class="product_card">
                <div class="product_block">
                  <a href="{{ product.url }}">
                    <img
                      src="{{ product.featured_image | image_url }}"
                      alt="{{ product.title }}"
                      loading="lazy"
                      width=""
                      height=""
                    >
                  </a>
                </div>

                <div class="product_content">
                  <h3>{{ product.title }}</h3>
                  <div class="price">{{ product.price | money }}</div>

                  <form method="post" action="/cart/add" id="custom-product-form-{{ product.id }}">
                    <input type="hidden" name="id" value="{{ product.variants.first.id }}">
                    <input min="1" type="number" id="quantity" name="quantity" value="1">
                    <button type="submit" name="add" class="custom-add-to-cart-btn">Add to Cart</button>
                  </form>
                </div>
              </div>
            </div>
          {% endfor %}
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>

<script>
  document.addEventListener('submit', function (e) {
    const form = e.target;

    if (!form.action.includes('/cart/add')) return;

    e.preventDefault();

    const formData = new FormData(form);

    fetch('/cart/add.js', {
      method: 'POST',
      body: formData,
      headers: {
        'X-Requested-With': 'XMLHttpRequest',
      },
    })
      .then((response) => response.json())
      .then(() => {
        // 🔥 OPEN SHOPIFY CART DRAWER (DAWN + MOST THEMES)
        if (window.Shopify && Shopify.theme && Shopify.theme.cartDrawer) {
          Shopify.theme.cartDrawer.open();
        }

        // Fallback for themes using <cart-drawer>
        const cartDrawer = document.querySelector('cart-drawer');
        if (cartDrawer) {
          cartDrawer.open();
        }

        // Refresh cart sections
        document.dispatchEvent(new CustomEvent('cart:refresh'));
      })
      .catch((error) => console.error('Add to cart error:', error));
  });
</script>

{% schema %}
{
  "name": "Custom Product Cards",
  "settings": [],
  "max_blocks": 1,
  "blocks": [
    {
      "type": "product",
      "name": "Product Card",
      "settings": [
        {
          "type": "product_list",
          "id": "product_item",
          "label": "Select product"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Product Cards"
    }
  ]
}
{% endschema %}
